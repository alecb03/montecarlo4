# readthedocs.yml

version: 2
build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

build:
  image: latest

python:
  install:
    - requirements: requirements.txt

conda:
  environment: docs/requirements.yaml